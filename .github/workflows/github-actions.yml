name: ooo-three Gitgub-Actions-Starter
run-name: ${{ github.actor }} ‘s ooo-three 🚀
on: [push]
jobs:
  myJob_01:
    runs-on: ubuntu-latest
    steps:
      - run: echo "🍏 This job's status is ${{ job.status }}."
